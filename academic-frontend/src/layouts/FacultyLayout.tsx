import { Outlet } from "react-router-dom"

/**
 * Placeholder wrapper for Faculty-only routes.
 * Will host the Faculty-specific sidebar in a later prompt.
 */
export default function FacultyLayout() {
  return <Outlet />
}
